# modsmith examples

Rendered scaffolds checked in for documentation, comparison, and smoke-testing modsmith itself.

| Directory | What it shows |
|---|---|
| [`minimal-multiloader/`](./minimal-multiloader/) | Multi-MC overlay scaffold — 2 MC versions × 2 loaders, no mod code beyond the entry point. Re-rendered from `minimal-multiloader.vars.json`. |

To regenerate an example after editing templates, see the `## Regenerating this example` section in the example's own README.
